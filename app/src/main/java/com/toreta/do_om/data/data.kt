package com.toreta.do_om.data

import android.widget.ProgressBar

data class dataOne(var imageView: Int, var title: String, var subtitle: String, var data : Int, var ondo : String)

data class dataTwo(var imageView: Int, var title: String, var subtitle: String)

data class dataThree(var imageView: Int, var user: String, var ranking: String)

data class dataFour(var notification : String)